program secret;

uses
  Forms,
  login in 'login.pas' {loginform},
  md5 in 'md5.pas',
  setsec in 'setsec.pas' {Form3};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(Tloginform, loginform);
  Application.CreateForm(TForm3, Form3);
  Application.Run;
end.
